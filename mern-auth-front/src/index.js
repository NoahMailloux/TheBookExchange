import React from "react";
import ReactDOM from "react-dom"; //renders react components
import App from "./App"

ReactDOM.render(<App />, document.querySelector("#root")); // inserts into root div element on index whatever the app returns